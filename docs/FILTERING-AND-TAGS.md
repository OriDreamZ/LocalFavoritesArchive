# 筛选与标签规范

收藏页面支持关键词、作者、媒体类型、日期、排序和多标签筛选。

多标签通过 `tag_ids` 重复参数传递，`tag_mode=all` 表示交集，`tag_mode=any` 表示并集。空标签表示不启用标签条件。列表接口和总数接口必须使用相同条件，以保证分页可靠。旧的 `tag_id` 参数继续兼容。

页面使用复选框选择标签，筛选关系使用“全部满足／任一满足”控件。筛选条件写入 URL，刷新后恢复；标签被删除或当前推文移除筛选标签时，结果重新计算并将页码限制在有效范围内。
