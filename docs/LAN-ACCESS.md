# 局域网访问规范

服务默认只监听 `127.0.0.1`，局域网访问默认关闭。需要时启动：

```powershell
local-favorites serve --lan
```

`--lan` 绑定 `0.0.0.0`，状态页和命令行显示可用局域网地址。局域网模式不进行身份验证，任何同网段设备都拥有完整管理权限，包括删除、同步、标签、设置和媒体重试。自定义客户端标记只用于降低跨站请求风险，不是身份认证。

程序不自动修改 Windows 防火墙。用户需要手动放行指定端口，并在不需要时关闭服务或取消防火墙规则。禁止端口映射、公网暴露和未经保护的反向代理。
