# Chrome 扩展采集规范

扩展默认提供“从当前位置继续”和“从头重新同步”两种模式。前者不刷新当前 Likes 页面，先读取当前已渲染的推文，再从当前滚动位置向下滚动；后者刷新页面后重新捕获网络响应。

DOM 采集只接受带状态链接和时间的推文，记录正文、作者、发布时间、原始链接和可用媒体。`blob:` 视频地址不可作为本地下载来源，真实视频地址仍以之后捕获的网络响应为准。DOM 批次与 GraphQL 批次都通过推文 ID 去重。
